# מדריך פריסה — צעד אחר צעד

מצב הפתיחה אצלך (נבדק): git מותקן · הריפו כבר אותחל ב-`~/hebrew-quest` על ענף `main` · 38 הקבצים כבר ב-staging · **אין** קומיט · **אין** זהות git · **אין** מפתח SSH · **אין** `gh` CLI.

לכן המסלול הנכון עבורך הוא **HTTPS + טוקן אישי**. זה נשמע מפחיד ולוקח 3 דקות פעם אחת, ואחר כך macOS זוכר את הטוקן לנצח.

⏱️ סה"כ: 15–20 דקות. אחרי זה כל עדכון עתידי הוא 3 פקודות.

---

## שלב 0 — החלטה אחת לפני שמתחילים

**הריפו חייב להיות ציבורי.** GitHub Pages בחשבון חינמי מפרסם רק מריפו ציבורי (פרטי דורש GitHub Pro בתשלום).

מה זה אומר בפועל: כל אחד יוכל לראות את **הקוד ואת התוכן** של האפליקציה. מה שלא יהיה שם: שום נתון של הבן שלך. ההתקדמות נשמרת רק במכשיר שלו (ואם תחבר Supabase — בדאטאבייס שלך, שאינו חלק מהריפו). גם השם שהוא מקליד נשאר במכשיר.

אם בכל זאת לא נוח לך שהריפו ציבורי — דלג לסוף, ל[מסלול חלופי: Netlify](#מסלול-חלופי--netlify-drop-בלי-חשבון-בלי-git).

---

## שלב 1 — זהות git (פעם אחת במחשב)

git מסרב ליצור קומיט בלי לדעת מי אתה. הרץ:

```bash
git config --global user.name "Omer Chehmer"
```

```bash
git config --global user.email "omer@travelier.com"
```

> אם אתה מעדיף שהמייל שלך לא יופיע בקומיטים הציבוריים, GitHub נותן מייל חלופי: אחרי שתפתח חשבון, לך ל-Settings → Emails → סמן *Keep my email addresses private*, והשתמש בכתובת בסגנון `12345678+username@users.noreply.github.com` שתופיע שם, במקום המייל האמיתי.

---

## שלב 2 — הקומיט הראשון

```bash
cd ~/hebrew-quest && git commit -m "מכרה המילים — גרסה ראשונה"
```

אמור להופיע משהו כמו `38 files changed, 4xxx insertions(+)`. לאימות:

```bash
cd ~/hebrew-quest && git log --oneline
```

---

## שלב 3 — יצירת הריפו ב-GitHub

1. היכנס ל-[github.com](https://github.com). אם אין לך חשבון — הרשם (מייל, סיסמה, אימות).
2. לחץ על **+** בפינה הימנית העליונה → **New repository**.
3. מלא:
   - **Repository name**: `hebrew-quest`
   - **Description**: אפשר להשאיר ריק
   - **Public** ← חובה (ראה שלב 0)
   - **אל תסמן** את `Add a README file`, `Add .gitignore` או `Choose a license`. הריפו חייב להיווצר ריק לגמרי, אחרת ה-push יידחה.
4. **Create repository**.

תגיע למסך עם הכותרת *"…or push an existing repository from the command line"*. אל תעתיק משם — הפקודות שלנו למטה מדויקות יותר.

---

## שלב 4 — יצירת טוקן (במקום סיסמה)

GitHub לא מקבל סיסמה רגילה בשורת הפקודה מאז 2021. צריך טוקן.

1. [github.com/settings/tokens](https://github.com/settings/tokens) (או: תמונת הפרופיל → Settings → Developer settings → Personal access tokens).
2. **Tokens (classic)** → **Generate new token** → **Generate new token (classic)**.
3. מלא:
   - **Note**: `mac-hebrew-quest`
   - **Expiration**: `No expiration` (או שנה — אז תצטרך לחדש)
   - **Scopes**: סמן את התיבה **`repo`** בלבד. זה מסמן אוטומטית את כל תתי-הסעיפים שלה. אל תסמן שום דבר אחר.
4. **Generate token**.
5. **העתק את הטוקן עכשיו** (מתחיל ב-`ghp_`). GitHub מציג אותו פעם אחת בלבד. הדבק אותו זמנית איפשהו — בעוד שתי דקות תשתמש בו.

> הטוקן הזה הוא סיסמה לכל דבר. אל תשלח אותו באימייל/וואטסאפ ואל תשמור אותו בקובץ בתוך הפרויקט.

---

## שלב 5 — חיבור ו-push

החלף `<USERNAME>` בשם המשתמש שלך ב-GitHub:

```bash
cd ~/hebrew-quest && git remote add origin https://github.com/<USERNAME>/hebrew-quest.git
```

```bash
cd ~/hebrew-quest && git push -u origin main
```

הטרמינל ישאל:

```
Username for 'https://github.com': ← הקלד את שם המשתמש שלך
Password for 'https://...':        ← הדבק כאן את הטוקן (ghp_...)
```

**הסיסמה לא תיראה בזמן ההקלדה** — לא הסמן, לא כוכביות, כלום. זה תקין. הדבק ולחץ Enter.

הפלט אמור להסתיים ב-`branch 'main' set up to track 'origin/main'`.

macOS ישמור את הטוקן ב-Keychain, ולא תישאל שוב.

**אם קיבלת שגיאה:**

| השגיאה | הסיבה והפתרון |
|---|---|
| `remote origin already exists` | הרצת את הפקודה פעמיים. הרץ `git remote set-url origin https://github.com/<USERNAME>/hebrew-quest.git` |
| `Authentication failed` | הדבקת סיסמה במקום טוקן, או שהטוקן בלי הרשאת `repo`. צור טוקן חדש. |
| `Updates were rejected` | יצרת את הריפו עם README. הרץ `git push -f origin main` (הריפו ריק ממילא, אין מה לאבד). |
| `repository not found` | טעות בשם המשתמש או בשם הריפו ב-URL. בדוק אות-אות. |

---

## שלב 6 — הפעלת GitHub Pages

1. בדף הריפו: **Settings** (הלשונית העליונה, לא ההגדרות של החשבון).
2. בתפריט הצדדי: **Pages**.
3. תחת **Build and deployment**:
   - **Source**: `Deploy from a branch`
   - **Branch**: `main` · תיקייה: `/ (root)`
4. **Save**.

רענן את הדף אחרי דקה־שתיים. למעלה יופיע פס ירוק:

> **Your site is live at** `https://<USERNAME>.github.io/hebrew-quest/`

**הקישור הזה הוא מה שאתה שולח לבן שלך.** הוא קבוע ולא ישתנה.

אם עברו 5 דקות ועדיין 404 — בדוק בלשונית **Actions** בריפו שהריצה *pages build and deployment* הסתיימה בירוק.

---

## שלב 7 — אימות שהכול עובד

פתח את הקישור **במחשב** קודם:

- [ ] נטען מסך פתיחה עם מכוש ושדה שם. אם המסך לבן/ריק — פתח קונסול (Cmd+Option+I) ובדוק שגיאות; כמעט תמיד זה קובץ חסר.
- [ ] הקלד שם → "בוא נתחיל" → נפתח מסך הבית עם מפת 28 הימים.
- [ ] "התחל" → נטענת תחנת אוצר מילים.
- [ ] פתח גם `https://<USERNAME>.github.io/hebrew-quest/parent.html` — אמור להופיע דשבורד או הודעה שאין נתונים במכשיר הזה. שתי התוצאות תקינות.

---

## שלב 8 — התקנה על האייפון של הבן

1. שלח לו את הקישור (וואטסאפ/הודעה).
2. הוא פותח אותו **ב-Safari**. חשוב: לא בכרום ולא בדפדפן שבתוך וואטסאפ — רק שם ההוספה למסך הבית עובדת. אם זה נפתח בתוך וואטסאפ, יש כפתור "פתח ב-Safari" בפינה.
3. כפתור השיתוף (ריבוע עם חץ למעלה) → גלילה למטה → **הוספה למסך הבית** → **הוסף**.
4. לסגור את Safari ולפתוח **מהאייקון** במסך הבית. עכשיו זה במסך מלא, בלי סרגלי דפדפן.
5. במסך הפתיחה — להקליד שם וללחוץ **"בוא נתחיל"**. הלחיצה הזו היא שמפעילה את הקול; iOS חוסם הקראה שלא באה מלחיצה של המשתמש. זו הסיבה שהמסך הזה קיים.

**בדיקת הקול:** בתרגיל הראשון של הכתיב אמורה להישמע מילה בעברית. אם לא נשמע כלום:
- ודא שהמכשיר לא במצב שקט (המתג בצד) ושהעוצמה למעלה.
- הגדרות → נגישות → תוכן מדובר → קולות → עברית → ודא שיש קול מותקן (Carmit).
- אם עדיין אין קול: האפליקציה עוברת לבד ל"מצב הבהוב" — המילה מוצגת לשנייה במקום להיקרא. התרגול נשאר תקף, פשוט פחות טוב.

---

## שלב 9 — מסך ההורה

**על המכשיר שלו:** לחיצה ארוכה של 2.5 שניות על אייקון המכוש בפינה של מסך הבית → הגדרת קוד בן 4 ספרות → נפתח קישור אישי. שמור אותו אצלך.

**מהטלפון שלך (דורש Supabase):** ראה את הפרק במדריך ב-[README](README.md#מסך-ההורה). בלי זה — הדשבורד עובד רק על המכשיר שלו.

---

## איך מעדכנים אחרי שינוי

בכל פעם שתשנה תוכן או קוד:

1. **תמיד** — העלה את מספר הגרסה ב-`sw.js`, שורה 4: `const VERSION = 'hq-v3';` (אחר כך v4 וכן הלאה). בלי זה המכשיר עלול להישאר עם גרסה ישנה במטמון.
2. בדוק שהתוכן תקין:

```bash
cd ~/hebrew-quest && npm run check
```

3. שלח:

```bash
cd ~/hebrew-quest && git add -A && git commit -m "תיאור השינוי" && git push
```

תוך כדקה־שתיים זה עולה. אם בטלפון שלו עדיין רואים את הישן — לסגור את האפליקציה לגמרי (החלקה למעלה) ולפתוח שוב. במקרה קיצון: למחוק את האייקון ממסך הבית ולהוסיף מחדש.

**ההתקדמות שלו לא נמחקת בעדכון** — היא ב-localStorage, לא בקוד.

---

## מסלול חלופי — Netlify Drop (בלי חשבון, בלי git)

אם אתה רק רוצה לראות את זה חי על הטלפון תוך שתי דקות, או שהריפו הציבורי מפריע לך:

1. [app.netlify.com/drop](https://app.netlify.com/drop)
2. גרור לחלון את **התיקייה** `~/hebrew-quest` כולה (ב-Finder: Cmd+Shift+H ואז גרור את התיקייה).
3. תוך שניות תקבל כתובת כמו `https://random-name-123.netlify.app`.

**המגבלה:** בלי חשבון, כל העלאה מחדש נותנת כתובת חדשה — כלומר האפליקציה על מסך הבית שלו תפסיק לעבוד בכל עדכון. פתיחת חשבון חינמי ב-Netlify פותרת את זה ומאפשרת גם לשנות את השם לכתובת קבועה.

לכן: Netlify מצוין לבדיקה מהירה היום, GitHub Pages נכון לחודש הקרוב.

---

## גיבוי ההתקדמות

שווה לדעת: Safari מוחק לפעמים נתונים של אתרים שלא נפתחו זמן רב. אפליקציה שהותקנה למסך הבית מוגנת יותר, אבל לא חסינה.

באפליקציה: **הגדרות → ייצא קובץ** שומר את כל ההתקדמות כקובץ JSON. שווה לעשות את זה פעם בשבוע, למשל בסוף כל קרב בוס. **ייבא קובץ** משחזר.
